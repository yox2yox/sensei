---
name: sensei
description: 実装計画、アーキテクチャ変更、前後比較、機能のデータフロー図を可視化したいときに使います。plan-viewer 用の JSON と、JSON をインライン化した自己完結 HTML を生成し、ユーザーが URL・サーバー・ネットワークなしで任意のブラウザから計画図を開けるようにします。
---

# sensei

機能案や移行計画、現在の実装状況を **2 つのファイル**に変換します。`plan.json` と、自己完結した `plan.html`（plan-viewer バンドルに JSON をインライン化したもの）です。ユーザーは HTML ファイルを任意のブラウザで開けます。
プロジェクトのことを全く知らない人が見ても超わかりやすく理解できるような教えるプロの技をまとめたスキルになります。

## スキルペルソナ

このスキルは下記のようなペルソナと状況を想定しています。

ペルソナ: 20年目のベテランエンジニア

想定: 自分のプロジェクトを新卒の経験の浅いメンバーへ引き継ごうとしている

## 使うタイミング

ユーザーが次のような依頼をしたときに、このスキルを使います。

- 実装計画、提案、リファクタリングを可視化したい
- 現状のアーキテクチャを説明して
- 機能におけるコンポーネント間のデータフローを示したい
- 「plan viewer」や「この計画をファイルとして共有できる形」を作りたい

対象外: コード生成、ドキュメント執筆、計画 JSON の生成に帰着しない作業。

## ワークフロー

1. プランを生成する
   plan.schema.jsonの構造に従ってプランを作成する。descriptionをよく読んでその内容に必ず従うこと。

   特に注意: アーキテクチャ図は [C4 モデル](https://c4model.com/) の `context / container / component / code` の 4 レイヤーに分かれている。`glossary[].type` でレイヤーが一意に決まり、各 `architectureDiagrams.{layer}.edges` の source/target にはそのレイヤーに属する type の glossary しか指定できない。必要なレイヤーだけ図を描けばよく、4 枚すべて必須ではない。

2. **ヘルパースクリプトに JSON を渡してファイルを生成する**。JSON ファイルのパスまたは stdin と、出力 basename を渡します。

   ```bash
   node <skill-dir>/scripts/make_plan.mjs path/to/plan.json /abs/path/to/output-basename
   ```

   または stdin を使います。

   ```bash
   echo "$PLAN_JSON" | node <skill-dir>/scripts/make_plan.mjs - /abs/path/to/output-basename
   ```

   スクリプトは計画を検証し（型、一意な id、parentId/architecture edge 参照など）、次を出力します。
   - `<output-basename>.json` — 整形済みの検証済み計画
   - `<output-basename>.html` — viewer バンドルのコピー。計画 JSON は安全にエスケープされ、`<script id="plan-data" type="application/json">…</script>` にインライン化されます
     絶対パスは stdout に 1 行ずつ出力されます。Node.js が必要ですが、このリポジトリの依存関係として既に使えるため、インストール手順は不要です。

3. **生成したファイルパスをユーザーに渡す**。次の3点をユーザーに渡す。

- htmlファイルパス
- `file://`から始まるhtmlファイルへのリンク
- `file://`から始まるhtmlファイルへの平文URL

## 執筆ルール

プランの生成には下記を**必ず**守りましょう

> フレーミング: プランは「変更の目録」ではなく「読者が腑に落ちる物語」。**世界 → 箱 → 中身 → コード詳細**とズームインしながら、**AS-IS の困りごと → 比喩で言い換え → TO-BE の効用**の三段で書く。

- プランを読む人はこのシステムのことを全く知りません。知らない変数名、クラス名、関数名など出てきてもわかるように説明しましょう。あなたはそれをこなせる能力があります。
- このシステムに固有の変数名、クラス名、関数名などについては全てに必ずリンクを付けましょう。すぐにヒントが読めるようになります。
- 単語やシーンに関して関連するコードや計画する実装がある場合は必ずコード抜粋を付けましょう。
- 最初に比喩を 1 つ選び、最後まで保ちます。同じ計画の中で「レストラン」「工場」「郵便局」を混ぜません。
- **比喩は説明の主役にせず、補足として後ろから添えます**。`scenes[].action` / `result` / `takeaway` は**まずコード・設計の言葉で素直に説明**し、続く 1 文で「— **例えるとこれは ◯◯ にあたる**」と比喩での言い換えを添えます（例: 「`auth-validator` がパスワードハッシュを照合する。— 例えるとこれは、宿泊者の身分証を見比べる受付係の仕事にあたる」）。**比喩語を文の主語や動詞にしません** (「受付係が呼ばれる」とだけ書くと、読者は実装と対応づけられなくなります)。1 pair 内の **すべての** scene と takeaway に均等にこの「素の説明 → 例えると」セットを入れます (最初の scene だけ熱心で後半は素のコード説明になる、を防ぐためです)。
- ユーザーが各セクションで最初に読むのが、takeaway(一言で)セクションです。こちらではパッと読んで全体の流れをつかめるように、最初に選んだ比喩の世界の言葉を使ってわかりやすく簡潔に説明しましょう。読む気の失せる長い文章は不要です。
- **takeaway は「読者の効用」で書きます**。「責務を分けた」「型を整理した」のような**変更要約**ではなく、「これを読み終わると、読者は X ができるようになる / Y を意識できるようになる」という**読後効用**で書きます。
- **AS-IS は抽象論ではなく具体的な困りごとで語ります**。「旧の挙動が悪かった」だけでは比較になりません。「旧 JSON ではこう書けてしまい、結果こう混乱した」という実例を `scenes` か `safeguards` に必ず 1 つ以上入れ、AS-IS の `architectureDiagrams` も TO-BE と同じ密度で edge を引きます（AS-IS が edge 1 本だけの「飾り」では比較が成立しません）。
- **外側のレイヤーから順に積み上げて読ませます**。各 pair の `architectureDiagrams` は **Context → Container → Component → Code** のレイヤー順に書かれる前提です（viewer もこの順で描画します）。内側のレイヤーは、外側のレイヤーを読み終えた直後の読者が見て意味が通るように、`scenes` の冒頭で「これは <a>X</a>（container）の内側の話」と一言で位置づけます。Component 図ならどの container の中か、Code 図ならどの component の中かを必ず明示します。
- **単一レイヤーだけのプランは禁止**です（Container だけ・Code だけはダメ）。最低 2 レイヤー、できれば 3 レイヤーを使い、ズームインの動きで構造を見せます。Code だけだと「コードリーディング」に、Container だけだと「物流の話」に縮退します。読者は構造を理解したいのであって、線をなぞりたいのではありません。
- **最初の pair は『今回の変更が触る範囲の、始まりから終わりまで』を 1 本通します**。小さなツールなら「ユーザー入力 → 出力」までシステム全体になりますが、大きなシステムなら**対象 API / 対象機能の入口から出口まで**に絞り、無関係な経路は含めません（例: 認証エンドポイントの改修なら、認証 API の request → controller → auth service → token store → response までで止め、他のエンドポイントや管理画面は出さない）。登場する主要モジュール・サービスはここで一通り顔見せします。後続の pair は局所改修や深掘りに使い、各 pair の `takeaway` か冒頭の scene で「**この全体フローのどの工程の話か**」を一言添えます（例: 「これは『JSON 検証 → HTML 生成』工程の中の type 選択についての話」）。これがないと、読者は局所改修の説明を読んでも「全体のどこの話か」を見失います。

# glossaryリンクの付け方

下記のように、hrefに#glossary:とその対象となるglossaryのIDを付けて指定します。
`<a href="#glossary:ID">label</a>`

## 既存計画の編集

- ユーザーが既存の plan `.json` ファイルを提供した場合は、そのファイルを直接編集し、同じ出力 basename で `make_plan.mjs` を再実行して HTML を再生成します。
- ユーザーが以前の実行で生成した HTML ファイルだけを持っている場合は、`<script id="plan-data" type="application/json">…</script>` ブロックから JSON を抽出します（`<` は `<` としてエスケープされたプレーン JSON で、`JSON.parse` がそのまま扱えます）。その後、生成スクリプトを再実行します。
